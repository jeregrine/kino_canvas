# Canvas Test

## Section

```elixir
Mix.install(
  [
    {:canvas_renderer, path: "~/projects/canvas_renderer"}
  ],
  force: true
)
```

```elixir
CanvasRenderer.new(300, 300)
|> CanvasRenderer.API.set_line_width(10)
|> CanvasRenderer.API.stroke_rect(100, 100, 100, 100)
|> CanvasRenderer.API.fill_rect(0, 0, 100, 100)
|> CanvasRenderer.API.fill(100)
|> CanvasRenderer.render()
```
